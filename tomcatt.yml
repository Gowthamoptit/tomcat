---
 - name: Deploy war  
   hosts: localhost  
   tasks:
     - name: start Tomcat
       command: "nohup /opt/tomcat/bin/catalina.sh start"
       
     - name: deploying war files from nexus directory
       get_url:
         url: 
         dest:  /opt/tomcat/webapps
 ...
